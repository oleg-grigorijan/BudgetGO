spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./src/test/resources/local/budgetgo_test
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.format_sql: true
